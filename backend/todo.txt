#Todo
# - make it possible to change motor IPs and ports and motion parameters from a config file or environment variables
# - make config pydantic so we never get a failure
# - add emergency stop
# - need to add a requirement.txt
# move types to their own file
# update to only allow reust from ffronted domain
# consider moving and having router if endpoints get too big
# add endpoint for if all four motors are connected
# modify tescONNECTION TO GET rs BACk from all motors and slaos include check health